.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
submission.zip
*.pgn

# training artefacts
/data/
/runs/
*.pt
*.ckpt
*.pgn.zst
# weights: not tracked in git during development; the final model ships via the
# zip build, and we decide how to version it (LFS / direct add) near the lock.
/weights/*.onnx
/weights/*.onnx.data
